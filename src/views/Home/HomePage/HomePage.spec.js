import HomePage from "./HomePage";

describe("Homepage", () => {
  it.todo(
    "should let user view at a glance how much they still have in their budget"
  );
  it.todo("should allow user to ");
});
